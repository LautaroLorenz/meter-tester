/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.seed = async function (knex) {
  // await knex('users').insert([
  //   { id: 1, name: 'Juan', surname: 'Perez', identification: 'Ingeniero' },
  // ]);
};
